---
phase: 01-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - vitest.config.ts
  - src/test/setup.ts
  - eslint.config.mjs
  - .prettierrc.json
  - .gitignore
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "npm run dev starts a Vite dev server without errors"
    - "npx vitest run exits 0 (empty suite passes)"
    - "npx prettier --check . exits 0 with zero violations"
    - "npx eslint . exits 0 with zero errors"
    - "npx tsc --version shows 5.8.x (not 6.0.x)"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "vitest"
    - path: "vitest.config.ts"
      provides: "Vitest test runner configuration"
      exports: ["default"]
    - path: "eslint.config.mjs"
      provides: "ESLint flat config with typescript-eslint"
      contains: "typescript-eslint"
    - path: ".prettierrc.json"
      provides: "Prettier formatting rules"
      contains: "trailingComma"
    - path: "src/index.css"
      provides: "Tailwind v4 import + CSS token definitions"
      contains: "--color-bg"
    - path: "src/test/setup.ts"
      provides: "jest-dom matcher registration"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles array"
      pattern: "setupFiles.*setup"
    - from: "eslint.config.mjs"
      to: "eslint-config-prettier"
      via: "last config entry (disables conflicting rules)"
      pattern: "prettierConfig"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "tailwindcss()"
---

<objective>
Scaffold a Vite + React + TypeScript project in the existing git repository and configure Vitest, Prettier, and ESLint as the quality gates all subsequent phases inherit.

Purpose: Phase 1 starts with a completely empty directory (no package.json). Nothing else can proceed until the project is scaffolded and quality tools are passing. This plan is the foundation.

Output: A working Vite dev server, passing `vitest run`, passing `prettier --check .`, and passing `eslint .` — the three quality gates from TOOL-01, TOOL-02, TOOL-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-pipeline/01-RESEARCH.md
@.planning/phases/01-data-pipeline/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" id="1-01-01">
  <name>Task 1: Scaffold Vite project and install all Phase 1 dependencies</name>
  <files>
    package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json,
    index.html, src/main.tsx, src/App.tsx
  </files>

  <read_first>
    - /home/alpine/sviewer/CLAUDE.md (stack constraints, TypeScript version requirement)
    - /home/alpine/sviewer/.planning/phases/01-data-pipeline/01-RESEARCH.md (scaffold command, pitfalls 1-4, installation commands)
  </read_first>

  <action>
**Step 1 — Scaffold into existing git repo (PITFALL 1: run from INSIDE /home/alpine/sviewer/):**

```bash
cd /home/alpine/sviewer
npm create vite@latest . -- --template react-ts
# Accept the "overwrite existing files" prompt. Only .gitignore will be overwritten.
# Do NOT accept from parent directory — that creates a new sviewer/ subdirectory.
```

**Step 2 — Pin TypeScript to 5.8.x (PITFALL 2: scaffold installs TS 6.0.x which CLAUDE.md forbids):**

```bash
npm install -D typescript@~5.8.3
npx tsc --version  # Must show 5.8.x
```

**Step 3 — Install all Phase 1 runtime dependencies:**

```bash
npm install clsx date-fns lucide-react downsample
npm install tailwindcss @tailwindcss/vite
```

**Step 4 — Install all Phase 1 dev dependencies:**

```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
npm install -D eslint @eslint/js typescript-eslint eslint-plugin-react-hooks eslint-config-prettier prettier
```

**Step 5 — Fix ESLint version (PITFALL 3: scaffold may install ESLint 9):**

After install, check `node_modules/eslint/package.json` version field. If it shows `^9.x`:
```bash
npm install -D eslint@^10.2.0
```
Delete any `.eslintrc.cjs` or `.eslintrc.js` files if they exist.

**Step 6 — Update vite.config.ts to add Tailwind v4 plugin:**

Replace the scaffolded `vite.config.ts` with:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

**Step 7 — Add test script to package.json scripts block:**

Ensure these scripts exist in package.json:
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

**Step 8 — Update src/index.css with Tailwind v4 import and CSS design tokens (from UI-SPEC):**

Replace all content in `src/index.css` with exactly:

```css
@import "tailwindcss";

:root {
  --color-bg: oklch(98% 0 0);
  --color-surface: oklch(94% 0 0);
  --color-accent: oklch(55% 0.2 250);
  --color-destructive: oklch(55% 0.22 25);
  --color-border: oklch(85% 0 0);
  --color-text-primary: oklch(15% 0 0);
  --color-text-secondary: oklch(45% 0 0);
}

@media (prefers-color-scheme: dark) {
  :root {
    --color-bg: oklch(12% 0 0);
    --color-surface: oklch(18% 0 0);
    --color-accent: oklch(65% 0.18 250);
    --color-destructive: oklch(65% 0.2 25);
    --color-border: oklch(28% 0 0);
    --color-text-primary: oklch(92% 0 0);
    --color-text-secondary: oklch(60% 0 0);
  }
}
```

**Step 9 — Simplify src/App.tsx to a placeholder that compiles cleanly:**

```tsx
import './index.css';

function App() {
  return (
    <div className="min-h-screen" style={{ background: 'var(--color-bg)' }}>
      <p style={{ color: 'var(--color-text-primary)' }}>SViewer loading...</p>
    </div>
  );
}

export default App;
```

**Step 10 — Verify scaffold result:**

```bash
cd /home/alpine/sviewer
npm run build  # Must exit 0 — confirms TypeScript compiles and Vite bundles
```
  </action>

  <verify>
    <automated>cd /home/alpine/sviewer && npm run build 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` exists at `/home/alpine/sviewer/package.json` and contains `"vitest"`, `"tailwindcss"`, `"downsample"`, `"lucide-react"`, `"date-fns"`, `"clsx"`
    - `npx tsc --version` output contains `5.8.` (not `6.`)
    - `vite.config.ts` contains `tailwindcss()` in the plugins array
    - `src/index.css` contains `--color-bg` and `@import "tailwindcss"`
    - `npm run build` exits 0 (no TypeScript errors, Vite bundles successfully)
  </acceptance_criteria>

  <done>Project scaffolded, all dependencies installed, dev build passing, design tokens in CSS.</done>
</task>

<task type="auto" id="1-01-02">
  <name>Task 2: Configure Vitest, Prettier, and ESLint quality gates</name>
  <files>
    vitest.config.ts, src/test/setup.ts, eslint.config.mjs, .prettierrc.json
  </files>

  <read_first>
    - /home/alpine/sviewer/.planning/phases/01-data-pipeline/01-RESEARCH.md (Pattern 8: Vitest config, Pattern 9: ESLint flat config, Pattern 10: Prettier config, Pitfall 7: eslint-config-prettier must be last)
    - /home/alpine/sviewer/.planning/phases/01-data-pipeline/01-VALIDATION.md (Wave 0 requirements)
  </read_first>

  <action>
**Step 1 — Create vitest.config.ts:**

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
    globals: true,
  },
});
```

**Step 2 — Create src/test/setup.ts:**

```typescript
import '@testing-library/jest-dom';
```

Ensure the `src/test/` directory exists before writing:
```bash
mkdir -p /home/alpine/sviewer/src/test
```

**Step 3 — Create eslint.config.mjs (flat config — ESLint 10 format, PITFALL 7: prettierConfig MUST be last):**

```javascript
import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import reactHooks from 'eslint-plugin-react-hooks';
import prettierConfig from 'eslint-config-prettier';

export default tseslint.config(
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    plugins: { 'react-hooks': reactHooks },
    rules: {
      ...reactHooks.configs.recommended.rules,
    },
  },
  prettierConfig,
  {
    ignores: ['dist/', 'node_modules/'],
  }
);
```

**Step 4 — Create .prettierrc.json:**

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100,
  "tabWidth": 2
}
```

**Step 5 — Format all source files with Prettier to ensure zero violations immediately:**

```bash
cd /home/alpine/sviewer
npx prettier --write .
```

**Step 6 — Run all three quality gates and fix any issues before proceeding:**

```bash
npx vitest run          # Should exit 0 (no tests yet = pass)
npx prettier --check .  # Should exit 0 (just formatted)
npx eslint .            # Should exit 0 — fix any errors reported
```

**Common ESLint issue:** If `src/App.tsx` has unused imports from scaffold (e.g., `import { useState }`), remove them. If `tsconfig.json` has `include` paths that don't match, update.

**If ESLint reports `@typescript-eslint/no-unused-vars` on scaffolded boilerplate:** Delete boilerplate code from `src/App.tsx` that triggers it (the simplified version from Task 1 should have none).
  </action>

  <verify>
    <automated>cd /home/alpine/sviewer &amp;&amp; npx vitest run &amp;&amp; npx prettier --check . &amp;&amp; npx eslint .</automated>
  </verify>

  <acceptance_criteria>
    - `vitest.config.ts` exists and contains `environment: 'jsdom'` and `setupFiles: ['./src/test/setup.ts']`
    - `src/test/setup.ts` exists and contains `@testing-library/jest-dom`
    - `eslint.config.mjs` exists and contains `typescript-eslint` and `prettierConfig` (as last non-ignores entry)
    - `.prettierrc.json` exists and contains `"trailingComma": "es5"` and `"singleQuote": true`
    - `npx vitest run` exits 0
    - `npx prettier --check .` exits 0 with zero violations
    - `npx eslint .` exits 0 with zero errors
  </acceptance_criteria>

  <done>All three quality gates (Vitest, Prettier, ESLint) passing with zero errors. TOOL-01, TOOL-02, TOOL-03 satisfied.</done>
</task>

</tasks>

<verification>
Run all quality gates together:

```bash
cd /home/alpine/sviewer
npx vitest run && npx prettier --check . && npx eslint . && npm run build
```

All four commands must exit 0.

Additional checks:
- `npx tsc --version` shows `5.8.x`
- `node_modules` contains `downsample`, `lucide-react`, `date-fns`, `clsx`, `tailwindcss`
- No `.eslintrc.*` legacy config files exist (only `eslint.config.mjs`)
</verification>

<success_criteria>
1. Vite project scaffolded in `/home/alpine/sviewer/` with no new subdirectories
2. TypeScript 5.8.x installed (not 6.x)
3. All runtime deps installed: clsx, date-fns, lucide-react, downsample, tailwindcss
4. All dev tooling deps installed: vitest, testing-library, jsdom, eslint 10, typescript-eslint, prettier
5. `vitest run` exits 0
6. `prettier --check .` exits 0
7. `eslint .` exits 0
8. `npm run build` exits 0
9. `src/index.css` has Tailwind v4 import and all 7 CSS design tokens (light + dark)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-01-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
